#include <stdio.h>

int min = 1000000;

int recurse(int dp[][size])
{
if(dp[i][j]==-1)
	recurse();
else
	rerturn dp[i][j];
}
 
void 

 
int main()
{
	int size;
	while(scanf("%d",&size)!=0)
	{
	int dp[size][size];
	int i,j;
	for(i=0;i<size;i++)
		for (j=0;j<size;++j)
			dp[i][j] = -1;
		//the array contains the min moves when ith row queen is matched with jth row queen for changing.

		
	

	}

	return 0;
}